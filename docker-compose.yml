services:
  booking:
    build: .
    container_name: booking-microservice
    env_file:
      - .env          # MONGODB_URI, DB_NAME, PORT
    environment:
      - NODE_ENV=production
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    restart: unless-stopped
